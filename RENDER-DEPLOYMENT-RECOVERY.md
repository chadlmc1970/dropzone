# DropZone Render Deployment Recovery Prompt

## Current Status (March 16, 2026)

**Deployment:** Blueprint created on Render, both services deployed but **dropzone-api returning 502 errors**

### Services Deployed
- **dropzone-api**: srv-d6s30h94tr6s73cfl240 (FAILING - 502 error)
- **dropzone-dashboard**: srv-d6s30h94tr6s73cfl24g (status unknown)

### Issue
- URL https://dropzone-api.onrender.com/health returns `Cannot GET /health` (HTML error page)
- 7+ minute response time with HTTP 502
- Service failing to start properly

### All Credentials Configured in Render

**Backend (dropzone-api):**
```
DATABASE_URL=postgresql://neondb_owner:npg_dL81nGHpXJEl@ep-wandering-forest-am2r9807-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
AUTH0_DOMAIN=dev-r72wyvkz0bcgxnmk.us.auth0.com
AUTH0_CLIENT_ID=zLerZ9cQKfMYu8H0bRF4mcB8WHutZSiR
AUTH0_CLIENT_SECRET=2VpHRW3fOirKe8PB2Rg0-UwSpTmRaECTTM-vxJhOKAFfYsK3A
AUTH0_AUDIENCE=https://dropzone-api
SPOTIFY_CLIENT_ID=c7542388e8dc4ee18d0496383e1d0443
SPOTIFY_CLIENT_SECRET=c91ecc3c07c34f78892ea45a1f84041a
(JWT_SECRET_KEY auto-generated by Render)
(CORS_ORIGINS and FRONTEND_URL set in render.yaml)
```

**Frontend (dropzone-dashboard):**
```
NEXT_PUBLIC_AUTH0_DOMAIN=dev-r72wyvkz0bcgxnmk.us.auth0.com
NEXT_PUBLIC_AUTH0_CLIENT_ID=zLerZ9cQKfMYu8H0bRF4mcB8WHutZSiR
(NEXT_PUBLIC_API_URL and callback URL set in render.yaml)
```

## Files in Repo
- ✅ [render.yaml](./render.yaml) - Blueprint configuration
- ✅ [backend/Dockerfile](./backend/Dockerfile) - Multi-stage Docker build
- ✅ [RENDER-SETUP-GUIDE.md](./RENDER-SETUP-GUIDE.md) - Setup documentation
- ✅ All backend code complete (Tasks 1-13)
- ✅ 24/24 tests passing locally
- ✅ Latest commit: 9ba29da (render.yaml added)

## Next Steps to Fix

1. **Check Render build/deploy logs via dashboard:**
   - Go to https://dashboard.render.com/web/srv-d6s30h94tr6s73cfl240
   - Look for build errors or startup failures
   - Check if Docker build completed successfully

2. **Possible issues to investigate:**
   - Dockerfile path issue (render.yaml has `dockerfilePath: ./backend/Dockerfile`, `dockerContext: .`)
   - Missing environment variable causing startup failure
   - Port binding issue (Dockerfile uses 8000, Render expects 10000?)
   - Module import path issue (using `backend.app.main:app`)

3. **Quick diagnostic commands:**
   ```bash
   # Test local Docker build
   cd /Users/I870089/dropzone
   docker build -f backend/Dockerfile -t dropzone-test .
   docker run -p 8000:8000 --env-file <(echo "DATABASE_URL=...") dropzone-test

   # Or check Render logs via CLI
   render logs --resources srv-d6s30h94tr6s73cfl240 --limit 100
   ```

## Team Context
- **Team:** dropzone (4 members)
- **Location:** ~/.claude/teams/dropzone/
- **Coordinator Dashboard:** http://localhost:3001 (running)
- **Backend agent:** Idle, waiting for deployment completion

## Recovery Action
Use DropZone skill immediately:
```
/dropzone-project
```

Then debug the deployment failure by checking Render dashboard logs or testing Docker build locally.
